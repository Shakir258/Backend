<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="nav mb-10">
            <a class="text-blue-600" href="/"> Go back to home</a>
            
        </div>
        <h1 class="text-3xl font-bold mb-10">All Users</h1>
        <div class="users flex flex-wrap gap-5 ">
            <% users.forEach((user) => { %>
                <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                <div class="w-full h-52 rounded-lg bg-zinc-600 overflow-hidden">
                    <img class="w-full h-full object-cover" src="<%= user.image %>" alt="">
                </div>
                <h2 class="text-2xl font-bold mt-3"><%= user.name %></h2>
                <h5 class="text-zinc-400"><%= user.email %></h5>
                <div class="flex gap-3 mt-10">
                    <a  class="text-zinc-300" href="/edit/<%= user._id%>">Edit user</a>
                    <a  class="text-red-600" href="/delete/<%= user._id%>">Delete use</a>
                </div>
            </div>
            <%})%>
        </div>
    </div>
</body>
</html>